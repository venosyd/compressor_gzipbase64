name: compressor_imagem
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.1+2

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  archive:
  clipboard:
  file_picker_cross:
  flutter:
    sdk: flutter
  opensyd_flutter:
    git: https://github.com/venosyd/opensyd_flutter

flutter:
  uses-material-design: true
